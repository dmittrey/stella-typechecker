language core;
extend with #sum-types, #unit-type;

fn toSum(x : Nat) -> Nat + Unit {
    return inl(x)
}

fn main(_ : Nat) -> Nat {
    return match toSum(succ(0)) {
        inl(n) => n
      | inr(_) => 0
    }
}

//SUCCESS
