language core;
extend with #records,#nullary-functions,#natural-literals;

fn make() -> {x : Nat, x : Nat} {  // Ошибка: дублирование имени x в типе записи
  return {x = 1}
}

fn main(a : Nat) -> Nat {
  return a
}
